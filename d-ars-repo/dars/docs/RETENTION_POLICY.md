# (이동됨) 개인정보 보관·파기 정책

이 문서는 **비어 있다.** 정본은 `docs/PRIVACY_RETENTION.md` 하나뿐이다.

2026-09-09 작업 중 같은 내용의 파일을 잘못 새로 만들었고, 이 실행 환경에서는 파일 삭제 권한이
없어 지우지 못했다. 정책이 두 곳에 흩어지면 한쪽이 반드시 썩으므로 여기에는 내용을 두지 않는다.
`tests/retentionscan.test.mjs` 가 이 파일에 정책 표(`|` 표)가 들어오는 것을 막는다.

**사람이 할 일:** 이 파일을 삭제해도 된다(삭제 후 위 테스트의 해당 케이스도 함께 정리).
